package week_15;

public class WhatIsVector {

      /*
    *
    *
    * In Java, a vector is a dynamic, resizable data structure that can store a collection of elements.
    *  It is similar to an array but can grow or shrink in size as needed.
    * Think of a vector as a flexible container, like a bag, where you can add or remove items easily.

Programming-wise, a vector is part of the Java Collections Framework and is represented by the Vector class.

import java.util.Vector;

public class VectorExample {
    public static void main(String[] args) {
        // Creating a Vector of integers
        Vector<Integer> numbers = new Vector<>();

        // Adding elements to the vector
        numbers.add(10);
        numbers.add(20);
        numbers.add(30);

        // Accessing elements
        int firstElement = numbers.get(0); // Retrieves the first element (10)

        // Modifying elements
        numbers.set(1, 25); // Sets the second element to 25

        // Removing elements
        numbers.remove(2); // Removes the third element

        // Checking the size of the vector
        int size = numbers.size(); // Returns 2

        System.out.println("Vector: " + numbers); // Output: Vector: [10, 25]
    }
}

    *
    * */


}
